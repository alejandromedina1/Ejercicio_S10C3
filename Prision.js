class Prision{
    constructor(x, y){
        this.x=x;
        this.y=y;
    }
    printPrision(){
        fill(255);
        stroke(0);
        strokeWeight(3);
        rectMode(CENTER);
        rect(this.x, this.y, 200, 200);
        rectMode(CORNER);
        stroke(0);
        strokeWeight(1);
    }
    isOver(px,py){
        returtn
    }


}